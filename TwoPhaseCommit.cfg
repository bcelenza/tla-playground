\* TLC Model Configuration for TwoPhaseCommit

SPECIFICATION Spec

\* Model values — TLC will create a set of 3 distinct resource managers
CONSTANT RM = {rm1, rm2, rm3}

\* Check these invariants in every reachable state
INVARIANT TypeOK
INVARIANT Consistency

\* Deadlock is expected — once everyone commits/aborts, the system stops
CHECK_DEADLOCK FALSE

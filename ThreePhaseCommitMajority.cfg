\* TLC Model Configuration for ThreePhaseCommitMajority

\* SPECIFICATION Spec

CONSTANT RM = {rm1, rm2, rm3, rm4, rm5}

INVARIANT TypeOK

\* Consistency is intentionally OMITTED here — it WILL be violated.
\* Uncomment the next line to see TLC produce a counterexample trace
\* showing a committed RM alongside an aborted RM:
\* INVARIANT Consistency

\* To check EventualConsistency (liveness), change the SPECIFICATION
\* to FairSpec and uncomment the PROPERTY line:
SPECIFICATION FairSpec
PROPERTY EventualConsistency

CHECK_DEADLOCK FALSE
